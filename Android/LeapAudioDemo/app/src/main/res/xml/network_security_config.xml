<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Enforce TLS for all network connections -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust system certificate authorities -->
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
